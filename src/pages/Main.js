import React from 'react';
import { Home, About, Task, Memory, Reference} from './';

const Main = () => (
	<main>
		<Home />
		<About />
		<Task />
		<Memory />
		<Reference />
	</main>
);

export default Main;
